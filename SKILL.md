---
name: website-production-standards
description: 统一网站项目的视觉模块、逐元素交互动效、联系方式浮层、返回顶部与 GitHub Actions 部署规范。每当 Codex 开始制作、改版、实现或审查网站、落地页、前端、WordPress 项目，或配置网站部署工作流时使用，除非用户明确指定其他方案。
---

# 网站制作默认规范

在开始任何网站制作、改版、前端实现或部署配置前加载本规范。将以下要求作为默认方案；如果项目需求、品牌设计、无障碍要求、性能约束或用户明确指令冲突，优先遵循更具体的要求，并记录被覆盖的默认项。

## AURA 自适应执行策略（兼容版）

AURA 只用于按任务复杂度和风险选择工作模式，不覆盖用户指令、系统/开发者约束、项目要求、验证要求、安全规则或外部操作授权。

### 复杂度与风险

- Level 1：单文件、小范围、可逆且低风险的文字、样式或明确机械修复。
- Level 2：目标明确、边界清晰的功能实现或中等规模重构。
- Level 3：跨文件或跨模块重构、架构/性能/部署调整、外部系统操作或高影响变更。
- Level 4：目标、受众或成功标准尚未明确的探索性任务。
- 复杂度与风险分开判断；删除/覆盖、凭据、客户数据、生产写入、安全/合规和不可逆变更按高风险处理，即使改动范围很小。

### 执行模式

- `DIRECT-EXECUTE`：Level 1 且风险低时，直接执行授权范围内的安全、可逆修改，并运行必要的最小验证。
- `LITE-CYCLE`：Level 2 时，先给出简要目标和假设，再执行和做定向验证。
- `FULL-CYCLE`：Level 3 时，先检查现状、研究约束、比较方案并规划，再执行、验证和复盘；部署、数据操作或其他外部写入仍需明确授权。
- `COLLABORATIVE-ITERATION`：Level 4 时，先收敛目标、受众和成功标准，再用可逆原型迭代；只有缺失选择会实质改变结果时才提问。
- 执行中发现范围或风险升级时暂停当前模式并说明原因；可以降级以提速，但不得因此跳过必要验证。

### 设计与代码判断

- KISS、YAGNI、SOLID 是代码设计的判断工具：优先简单、可维护、不过度预留的方案，仅在确有复用或扩展需求时抽象。
- 网站任务还必须同时考虑用户需求、品牌、响应式、可访问性、性能、SEO、安全和部署约束；不能以“简洁”牺牲这些质量目标，也不能为静态页面过度抽象。

### 协作、静默与自动化边界

- `Silent` 只表示减少非必要的进度消息，不表示跳过测试、构建、运行、文档、验证或交付摘要；项目规范、用户要求和风险判断要求这些步骤时必须执行并报告。
- 不输出私有思考链；用简洁的依据、假设、计划、风险和验证证据说明决策。除非用户询问，不强制输出模型版本或知识截止日期，也不猜测这些元信息。
- 仅对高置信、低风险、可逆且已获授权的操作自动化。格式、类型或语法等局部确定性问题可以直接修复；逻辑、架构、安全、客户数据、凭据、删除覆盖和外部状态变更必须先报告并按授权边界处理。
- 发现需求变更时评估对范围、数据、部署和验收的影响，选择增量调整或重新规划；不把“完成任务”理解为可以扩大操作范围。
- 每次任务结束给出一行变更摘要、已执行的验证、未运行的检查和未解决事项。注释和日志优先遵循项目现有语言与用户要求；默认使用中文，但不强行改写第三方、生成文件或已有国际化内容。

## 开工前

- 识别项目类型：WordPress、自建后端 API、第三方 API 或纯前端项目；按代码证据分类，不要仅按目录名称、框架名称或 `.output/server` 判断。
- 根据项目类型按需读取对应 reference；如果样例标签与代码证据冲突，以实际调用链和部署脚本为准：
  - [WordPress](references/website-types/wordpress.md)：参考 [daqing](https://github.com/wuyoustudio/daqing.git) 的主题、CPT、seed、GEO 与 WordPress 对接和 Actions 部署逻辑。
  - [自建后端 API](references/website-types/self-hosted-api.md)：参考真正的后端判断标准；[mumen](https://github.com/wuyoustudio/mumen.git) 当前是外部 API 客户端，文档中记录了这一误分类边界。
  - [第三方 API](references/website-types/third-party-api.md)：参考 [feidekeNew](https://github.com/wuyoustudio/feidekeNew.git) 的 Orchard GraphQL、媒体代理、迁移和缓存逻辑。
  - [纯前端](references/website-types/frontend-only.md)：当前无指定样例，按静态数据、构建产物和静态托管边界判断。
- 检查现有页面结构、组件、设计系统、断点、构建命令和 `.github/workflows/`，优先复用既有规范。
- 确认部署目标、目标目录和所需密钥。缺少联系方式或配置值时使用占位配置并提醒用户，不要编造真实信息。
- 直接在 `main` 分支上修改，不创建或切换分支。
- 默认不创建隔离 worktree；个人开发时直接在当前 `main` 工作区推进任务。
- 每次打开项目或开始网站任务时，先检查仓库根目录、当前分支、上游分支、工作区和未跟踪文件，获取远端状态并自动同步最新版本；工作区干净且远端只是单纯领先时执行 `git pull --ff-only`。如果存在未提交改动、未跟踪文件、分叉或冲突，暂停自动 pull，保护本地文件并报告，不执行 `reset`、`clean`、强制覆盖或未经确认的自动合并。
- 检查并维护仓库根目录的 `.gitignore`：手动创建的 Superpowers、Codex 和其他开发辅助目录/临时文件默认忽略，例如 `.superpowers/`、`superpowers/`、`.codex/`、`.workbuddy/`、`.playwright-mcp/`、`tmp/`、`temp/`、`*.local.*`、`*.scratch.*`、`*.tmp` 和 `*.bak`。
- 默认每个网站仓库根目录下有两个固定文件夹，二者用途与版本控制策略不同：
  - `建站资料`（中文命名）：存放网站**开始制作前**的参考资料（素材、文案、原稿、favicon 等），属外部输入；将 `建站资料/` 加入 `.gitignore` 不纳入版本控制（规则只对尚未纳入版本控制的文件生效，不自动移除已跟踪内容）。
  - `docs/`（英文命名）：存放网站**制作过程中**的规划、注意点、设计规格、实施计划与验收记录，属项目特有参考；**纳入版本控制，不加入 `.gitignore`**。结构与用法见 [项目文档规范](references/project-docs.md)。
- 新建网站时在 `docs/` 建立并填写规划、注意点与设计/QA 记录；更新或改版网站时**先从 `docs/` 读取现有架构与验收基线再动手**，改动后同步更新（结构与用法见 [项目文档规范](references/project-docs.md)）。
- 不自动执行 `git push`；只有用户明确要求时才推送。不要把 Git 推送与 Actions 向目标网站目录的部署混为一谈。

## Logo 与 favicon

- 所有形式的网站（WordPress、自建后端 API、第三方 API 和纯前端）默认从当前项目对应的“建站资料”中选择文件名以 `favicon` 开头的图片作为站点 favicon（Logo 图标），例如 `favicon.ico`、`favicon.svg` 或 `favicon-32x32.png`；按项目类型在 HTML `<head>`、框架 head 配置或 WordPress 主题中引用随项目部署的实际资源。
- 存在多个候选时，按建站资料标注、项目现有配置和实际用途选择；如果仍无法判断，提醒用户确认，不能按搜索顺序随机取第一个。没有匹配资源时记录缺失并提醒用户补充，不使用固定地址、其他项目资源、`/favicon.ico` 或自行猜测 `logo.svg`、`logo.png` 等替代资源。

## 默认页面模块

- 默认加入至少一个设计感明确的图文卡片模块，并让它参与页面滚动展示节奏。卡片可用于产品、服务、优势、案例、流程或新闻内容，但应与页面主题匹配。
- 默认加入鼠标悬浮特效，为按钮、链接、卡片、图片、社交图标等可交互元素提供轻量的颜色、阴影、位移、缩放或边框反馈。
- 卡片默认按可交互组件处理：鼠标悬浮时显示小手指针（`cursor: pointer`），并确保卡片有对应的点击行为或链接；纯展示卡片没有交互行为时，不要用小手造成误导。
- 凡是带有鼠标浮动动效的模块，鼠标移上去时默认显示小手指针（`cursor: pointer`），因为动效本身在向用户暗示该元素可交互；这类模块一般以卡片形式出现，但也适用于其他带悬浮反馈的可点击区域。纯展示模块若确实没有点击行为，不要为了动效而加小手造成误导。
- 保持层级、间距、对比度和移动端可读性；动效不能遮挡、挤压或延迟用户获取关键信息。

## 案例列表与详情

- 开始制作案例模块前先定义统一展示模型，至少明确标题、客户、行业、服务范围、摘要、封面、详情 URL、语言、项目链接和证据资产；列表卡片与详情页必须读取同一份规范化数据，不要各自硬编码。
- 行业筛选和分页状态必须写入可分享的 URL；切换筛选时回到第 1 页，刷新、后退和直接打开链接后仍能恢复状态，并提供无结果、加载失败和图片失败状态。
- 案例卡片统一图片比例、标题层级、标签位置和点击区域，图片预留宽高或 `aspect-ratio` 防止布局跳动，并为有意义的封面提供准确的替代文本。
- 案例详情默认按“客户背景 → 目标/挑战 → 信息架构或方案 → 关键页面/交付物 → 结果与证据 → 项目链接/CTA”组织；视觉风格、色彩和行业语气应服务于受众与目标，不把某一个参考站点的渐变、大图或动效当成通用答案。
- 案例经验、视觉方案和交互建议必须写成条件规则：明确受众、证据状态、语言/地区、设备或业务目标满足何种条件时采用何种视觉手段；不把大图、渐变、动效、图表或图标当成行业通用答案。
- 案例内容必须显式区分“已核实事实”“待补信息”和“项目专属内容”：已核实项附来源、时间范围、项目角色和授权状态；待补项不得作为事实主张、资质、指标或证据展示；项目专属项不得直接升级为跨项目默认规则。
- 结果、客户规模、行业覆盖、转化提升等事实必须记录来源、时间范围、项目角色和授权状态；未核实的指标、资质和客户信息只能标记为待补，不得自行补写。
- 已发布的详情页至少要有一项经过确认的项目证据（关键页面、产品图、流程图、视频或明确交付物）；只有宣传性说明而没有证据资产时，先标记内容待补，不要把它当作完整案例对外展示。
- 详情页首屏在桌面和移动端都应同时呈现标题、摘要、主要链接和至少一项项目证据；长图、视频或大面积渐变不能制造大段空白，也不能被移动端固定咨询栏遮住。
- 中文、英文和多语言案例保留真实的原文语气、单位、行业术语和语言切换路径；不要只把中文文案机械翻译后沿用同一套断行、字号和 CTA。
- 交付前至少抽查一个图片丰富案例、一个内容待补案例和一个多语言案例，覆盖 1440px 与 390px 视口、直接详情 URL、筛选/分页恢复、图片加载、外链、键盘焦点和控制台弃用警告。

## 网站宽度与自适应布局

- 默认采用“最大内容宽度 + 流式总内边距”的策略，不将整个页面的根字号直接绑定到 vw。优先使用 `clamp()` 控制间距、字号和组件尺寸，避免在断点处突然跳变。
- 默认使用以下宽度与字号 token；`--container-inset` 表示左右两侧内边距的合计值，不是单侧值：

  ~~~css
  :root {
    --container: 1440px;
    --container-inset: clamp(32px, calc(13.52vw - 18.7px), 176px);
    --text-body: clamp(15px, 1.08vw, 17px);
    --text-body-lg: clamp(16px, 1.2vw, 19px);
  }

  body { font-size: var(--text-body); }
  .container { width: min(calc(100% - var(--container-inset)), var(--container)); margin-inline: auto; }
  ~~~

- 在以下常用视口下检查容器结果，作为实现时的基准值：
  - 1920px：内容区 1440px，两侧约 240px。
  - 1440px：内容区约 1264px，两侧各 88px。
  - 1024px：内容区约 904px，两侧约 60px。
  - 768px：内容区约 683px，两侧约 43px。
  - 375px：内容区约 343px，两侧 16px。
- 视口缩小时，容器通过 `clamp(32px, calc(13.52vw - 18.7px), 176px)` 连续收窄，不得出现横向滚动。移动端不要再覆盖为固定的 `.container { width: calc(100% - 32px); }`；保留同一套流式公式，让最小总内边距自然收敛到 32px。
- 网格子项统一使用 `minmax(0, 1fr)`，避免长标题、按钮或图片撑破容器。图片卡片设置稳定的 `aspect-ratio`，配合 `object-fit: cover` 或明确的 `contain`；产品目录类图片默认可采用 4 / 3，详情主图按模块比例单独定义。
- 默认响应式层级如下，具体组件可根据内容密度微调，但必须保持从宽到窄的连续收缩：
  - min-width: 1400px：宽屏内容卡片默认 4 列；产品展示默认显示 8 个，形成 4×2。
  - 769px–1399px：产品展示默认 3 列并显示 6 个；需要更紧凑时，分类卡片可提前切换为 2 列。
  - 769px–1000px：产品展示切换为 2 列并显示 4 个；主视觉、产品详情和联系模块可保留双栏，但必须检查最小列宽。
  - max-width: 900px：主视觉、内页首屏、产品详情、联系区和内容图文区切换为单栏；统计条和联系渠道等短内容可使用 2 列。
- max-width: 768px：导航、产品网格、分类网格、服务卡片和其他密集模块切换为单列；保持 32px 总内边距下限并确保触控区域足够大。
- Hero 等双栏主视觉在 min-width: 1001px 时保持双栏；max-width: 1180px 时收紧列宽与视觉高度，max-width: 900px 时切换单栏。需要桌面端固定换行的文字，在 max-width: 1000px 时取消强制换行。
- 断点检查至少覆盖 1920px、1440px、1024px、768px 和移动端宽度；检查容器宽度、列数、图片高度、标题换行、按钮溢出和横向滚动。

## AI 文案字号

- AI 生成的中文、英文及中英混排可见文字默认采用紧凑、易读的语义字号层级，不要为了强调或填充空间使用过大的文字；正文优先保持约 16px 级别，并沿用现有 `clamp()` 流式字号体系。
- 中文、英文和全大写英文使用同一语义角色的字号，不因英文看起来稀疏、字符较少或需要突出而单独放大；适配触屏尺寸应通过点击区域和内边距解决，不要放大按钮文字。
- 普通可见 H1/主标题桌面端默认不超过 36px、移动端不超过 24px；Hero、区块标题、卡片标题和 CTA 也必须有明确的字号上限。标题放不下时优先缩短 AI 文案、调整换行或布局，不要继续放大字号。

## 间距与行距

- 控制文字行高、段落间距和模块之间的垂直留白，默认采用紧凑、易读的节奏，不要出现参考截图中那样的大面积空白。
- 以用户提供的参考截图为基准，行间距、段落间距和模块垂直间隔默认约取截图效果的 50%，再根据字体、内容长度和响应式断点做小幅调整。
- 优先通过统一的 spacing token、`line-height` 和组件间距控制节奏，避免在单个模块中随意堆叠过大的 `margin`、`padding` 或固定高度。
- 交付前检查连续模块之间是否有不必要的空白，确保页面信息密度与视觉层级清晰。

## 导航与滚动

- 页面滚动时默认让主导航固定在视口顶部，使用 `position: sticky` 或等效的 fixed header 实现，确保用户始终能访问主要导航和关键操作。
- 页面从顶部开始滚动后，导航切换为紧凑状态，适当缩小高度和上下内边距；缩小幅度以保持品牌识别、文字可读性、Logo 清晰度和足够的触控区域为准，不要压缩到难以操作。
- 固定导航必须设置清晰的层级、背景和阴影/边框，避免与页面内容混在一起；内容和锚点滚动要预留导航高度，不能被导航遮住。
- 移动端保持菜单按钮、展开层和安全区域可用；检查固定导航不会遮挡首屏内容、表单控件或浮动联系方式。

## 顶部导航栏目

- 顶部主导航固定栏目最多 7 个（含“关于我们”的下拉子项不额外占用顶级栏目名额）。
- 默认固定栏目及中英文对应关系：首页 / Home、关于我们 / About、产品展示 / Product、新闻资讯 / News、联系我们 / Contact。
- 生产型企业默认用“产品展示 / Product”承载产品；服务业默认改为“服务项目 / Service”承载服务内容，不使用“产品展示”。
- 栏目超过 7 个或属于次要栏目时，收纳进“关于我们 / About”下拉菜单，不横向增加顶级栏目数量。
- 多语言站点导航保持中英文标签一一对应，栏目顺序与结构在各语言版本间保持一致。

## 内页横幅（Banner）

- 在主导航（内页导航）下方，除非用户特别说明，所有内页默认加入一个高度约 300px–400px 的横幅（Banner）。
- 横幅用于承载当前内页标题、核心引导文案或主题视觉，作为进入正文内容前的首屏过渡；高度随视口和内容做适度收缩，移动端可适当降低，但不应明显低于 300px 下限。
- 横幅不得与固定导航重叠或相互遮挡；固定导航在滚动收缩状态下，横幅顶部需预留导航高度偏移，保证标题可见且锚点跳转不被遮住。
- 横幅内文字默认沿用全站紧凑字号上限（普通 H1 桌面端不超过 36px、移动端不超过 24px），保持与页面整体视觉层级一致，不要因横幅而放大标题。

## 滚动与元素动效

- 为每个语义元素设计对应动效，不要让整块 section 或整张卡片作为一个整体同时运动。标题、标签、图片、正文、按钮、图标等应分别处理，必要时使用短距离、短时长的错峰进入。
- 让元素进入视口后稍作停顿再播放，不要在页面刚滚动到元素边缘时立即触发。默认使用约 200–400ms 的短延迟或等效的可读性停顿，并根据页面节奏调整。
- 默认让进入动效只播放一次，避免用户轻微上下滚动时反复闪烁；只有确有设计需要时才重复播放。
- 控制动效幅度和并发量，优先使用淡入、轻微位移、缩放或渐进显示，确保用户先看清内容再看到动效。
- 实现 `prefers-reduced-motion` 适配：用户要求减少动效时，缩短或移除非必要动画，保留内容和操作反馈。
- 为触屏设备提供等价反馈；不要只依赖 hover 才能发现或使用功能。
- 使用 Lenis 等第三方动效库时，以项目锁定版本的文档和类型为准，交付前清理弃用参数与控制台警告；当前 Lenis 配置应优先用 `smoothWheel` 替代已弃用的 `smooth`（仍以锁定版本为准），不要直接复制参考网站中过时的配置。
- 模块若为纯色背景，默认适当添加粒子动态特效（如漂浮粒子、光点、连线等），避免大面积纯色显得呆板；粒子默认使用 **canvas** 实现（纯 CSS 小范围点缀不禁止，但全屏/大量粒子优先 canvas，避免大量 DOM 节点影响性能），密度和速度保持克制，不遮挡文字、按钮或关键信息，并在 `prefers-reduced-motion` 下停止或大幅简化。

### 一般网站的主要动效触发器

每个动效先定义“触发器 → 状态变化 → 交互目的 → 最终状态/可中断规则 → 降级方案”，再选择 CSS、`IntersectionObserver`、`requestAnimationFrame`、CSS Scroll-driven Animation 或第三方库；不要先选库再强行寻找使用场景。

| 类型 | 常见触发器 | 默认实现与验收重点 |
| --- | --- | --- |
| 页面进入/滚动揭示 | 首次加载、路由进入、元素进入视口 | 内容默认可读；使用 `opacity`/`transform` 做一次性淡入、上移或错峰揭示，快速滚动和反向滚动不闪烁，脚本失败时不把内容留在 `opacity: 0`。 |
| 吸顶与滚动状态 | 页面滚动超过阈值、锚点跳转 | 导航可由透明叠加态切换为实体背景、阴影或紧凑高度；预留 header 偏移，不能因高度变化造成布局跳动或遮挡焦点。 |
| 滚动联动/视差 | 滚动进度、图片或标题进入指定区间 | 背景大字、图片和前景卡片可以使用有限幅度的不同速度或横向位移，但只优先动画 `transform`/`opacity`；不影响阅读、不制造横向滚动，减少动效时直接显示最终布局。 |
| 计数/进度 | 指标进入视口、步骤完成 | 只对有解释价值的数字使用递增或进度动画；最终数值必须存在于语义内容中，减少动效或失败时直接显示最终值，不人为制造等待。 |
| 鼠标悬浮/键盘聚焦 | 按钮、卡片、导航、图片、提示项 hover 或 `focus-visible` | 颜色、边框、阴影、轻微缩放或预览图可在约 150–300ms 内过渡；focus 必须有等价反馈，触屏不能依赖 hover，状态变化不能撑开布局。 |
| 点击/按压 | 菜单、标签、手风琴、弹窗、提交按钮 | 用语义化控件和 `aria-expanded`/`aria-pressed` 等状态驱动；提供按压反馈，支持 Enter/Space、取消和重复点击保护，动画可被新操作中断。 |
| 导航/标签切换 | 下拉、Mega Menu、标签页、路由或锚点切换 | 用淡入、滑入或共享元素保持空间连续；同步 active、URL、滚动位置和焦点，支持键盘、触摸和 Esc 关闭，不能只以悬浮显示关键信息。 |
| 轮播/视频 | 自动播放、上一张/下一张、拖拽、进入视口、页面切后台 | 视频默认静音、带 poster、字幕/文字替代和暂停控制；轮播提供当前页、前后切换和暂停，获得焦点、用户操作或页面不可见时暂停；失败时保留静态内容。 |

## 首页轮播图

- 首页轮播图若是纯图片（未提供手机版），移动端默认等比例缩小图片（`object-fit: contain` 或保持宽高比缩放），不裁切关键内容，也不强行拉伸铺满，确保整张图在小屏可见。
- 若单独提供了手机版轮播图，则按断点切换为使用手机版图片，桌面端仍用桌面版；两套图片各自保持自身比例与质量，不互相替换或缩放错配。
- 轮播仍须满足自动播放、前后切换、暂停、当前页指示和失败静态降级等通用要求；切换桌面/手机版图片时不制造布局跳动或横向滚动。
| 加载/表单反馈 | 图片、字体、接口、提交、校验、成功/失败 | 用骨架、进度、内联错误或成功状态表达原因；预留媒体尺寸防 CLS，不能用无限 spinner 或抖动代替信息，失败后有重试或静态降级。 |
| 触摸/手势 | 点击、滑动、拖拽、触摸取消 | 用 Pointer Events 和位移阈值区分点击/滑动，保留页面原生滚动，提供按钮等价操作；触控目标约 44×44 CSS px 以上，不能产生误触或卡住的 hover 态。 |

### 海威参考站点的动效拆解

对 [海威官网](https://www.hainwei.com/) 的桌面端 1440px 与移动端 390px 进行实际浏览后，提炼以下“触发关系”，不复制其具体素材、颜色、文字、动画时长或滚动阈值：

- Hero 使用全幅生产场景视频/画面循环，文字和 CTA 保持在前景；实现时应提供静态封面、静音/暂停、移动端和低网速降级，重要信息不能只存在视频里。
- 桌面端导航初始叠加在 Hero 上，滚动后切换为白色实体导航并改变 Logo 对比度；这是“滚动状态切换”，不是把导航简单固定后始终保持同一外观。
- 产品模块把车辆、产品详情卡和应用标签编排成滚动叙事：桌面端呈现卡片横向展开/停留，移动端退化为纵向卡片流；内容必须先于动效可读，不能为了复刻桌面视差让移动端出现横向溢出。
- 蓝色产品模块使用多张大图卡片、当前序号和前后控制形成轮播/滚动联动；必须同时支持按钮、键盘和触摸，不让自动切换抢走用户正在阅读的卡片。
- 关于模块使用大号背景词与前景内容形成相对位移，并在统计数字进入视口后递增；视差幅度应受限，数字减少动效时直接呈现最终值。
- 导航项悬浮后展开 Mega Menu，子项切换同步更换预览图；预览图只能是辅助信息，菜单还要支持 focus、点击、Esc、触摸和图片失败状态。按钮、关于页标签和右侧联系方式另有颜色填充、图标高亮或文字展开等悬浮反馈。
- 移动端没有 hover：菜单、标签、卡片和联系方式必须用点击/聚焦提供等价状态，固定入口不能遮挡内容；桌面端看到的滚动停留、横向排列和悬浮预览不能直接照搬到手机。

### 动效交付验收

- 在桌面端与 390px 移动端分别验证进入视口、快速滚动、反向滚动、吸顶、视差、计数、轮播、悬浮、focus、按压和失败状态。
- 开启 `prefers-reduced-motion: reduce` 后，移除非必要视差、连续运动、自动轮播和大幅缩放；内容、最终数值、焦点和操作反馈仍然可用。
- 用 `transform`/`opacity` 和批量读写控制帧耗时；避免在高频 `scroll` 中同步读写布局，避免动画 `width`、`height`、`top`、`left`；图片、视频、字体和动态卡片预留尺寸。
- 验证键盘 Tab/Shift+Tab/Enter/Space/Esc、屏幕阅读器语义、触摸操作、页面切后台、网络/媒体失败和 JS/动效库不可用时的静态回退；不把“动画播放完”作为获取内容或完成任务的前置条件。

## 浮动联系方式

- 桌面端默认将整组右侧浮动联系方式（包括展开面板）脱离内容容器，贴在浏览器最右侧，使用 `position: fixed` 或等效实现并设置 `inset-inline-end: 0`（LTR 页面即 `right: 0`），同时垂直居中；确保不遮挡主要内容、表单、Cookie 提示或关键操作。
- 国内项目默认准备微信二维码、手机号和邮箱入口；国外项目默认准备 Facebook、TikTok、LinkedIn 和 YouTube 入口。根据项目面向地区选择对应集合，不要同时无理由展示两套。
- 只展示项目实际提供的联系方式；缺少账号、号码或邮箱时隐藏该项，或保留不可操作且明确标注“待补充”的占位，不得生成虚假账号、号码或邮箱。
- 缺少二维码时，开发/验收可使用仓库内 `assets/qr-placeholder-test.png`（编码内容为 `test`）作为可替换占位；必须标注“测试二维码，仅用于占位，不是联系方式”，不可点击或作为真实入口，生产上线前替换为项目实际二维码或隐藏该项，不得生成其他二维码。
- 为图标、二维码和展开/收起控件提供可访问名称、键盘焦点状态和清晰的 hover/focus 反馈。
- 移动端将右侧浮层调整为不遮挡内容的折叠入口、底部入口或其他响应式布局，保持可用性。
- 移动端的底部 CTA、客服气泡、电话入口和返回顶部控件应合并层级或预留安全间距，不能互相覆盖，也不能遮住表单、案例图片或主要按钮。
- 右侧浮动框底部默认常驻一个返回顶部箭头，作为整组浮动框的一部分随之一起固定（不另起独立组件）。页面处于顶部时隐藏该箭头；用户向下滚动超过合适阈值后显示，避免在顶部占据视觉空间。
- 点击返回顶部箭头后平滑滚动到网页顶部，并提供可访问名称、键盘操作和可见 focus 状态；移动端确保触控区域足够大。

## Actions 标准化部署

- 所有网站项目默认通过 GitHub Actions 部署。
- 具体的数据源、API 边界、构建产物、迁移、回滚和验收要求，先按上面的项目类型 reference 执行，再套用本节通用 Actions 规则。
- WordPress 项目：在仓库存放主体（主题/站点主体代码）和插件，使用 Actions 将对应内容推送到目标 WordPress 目录。
- WordPress 客户服务器部署前置检查：当目标服务器由客户提供时，在 push 或触发 Actions 自动部署之前，必须使用本机保存且已确认对应服务器的 SSH alias/config/key 做只读预检。先核对解析出的主机、端口、登录用户、环境和 `known_hosts` 指纹；使用 `BatchMode`、`IdentitiesOnly` 和 `StrictHostKeyChecking=yes`，不得输出私钥、密码或密钥变量值。
- 预检必须在实际部署用户上下文确认远端 `rsync` 和 WP-CLI 可执行并能返回版本信息，例如检查 `command -v rsync` + `rsync --version`、`command -v wp` + `wp --version`；只确认 SSH 能登录或只检查 `command -v` 不算通过。主机、用户、工具缺失/不可执行/版本不符合要求或环境无法核对时，必须阻止 Actions，不得先触发部署。
- WordPress 客户服务器默认使用 PHP 8.2：预检还需确认远端 `php -v` 与 WP-CLI 报告的 PHP 版本为 8.2，并将 PHP 版本纳入阻止自动部署的条件（版本不符或 `php -l` 报错时不得部署）。开发电脑也默认安装 PHP 8.2，本地 lint/seed 与线上保持一致。
- 本机 SSH 预检不等于 GitHub Actions runner 已具备部署条件；还要确认 workflow 使用的 SSH 密钥、网络访问、远端用户、`ssh`/`rsync` 和 WP-CLI/PHP 运行条件与预检目标一致。任何一项不一致都要先报告并修正，不能把本机检查结果直接当作线上部署成功。
- 自建后端 API 项目：使用 Actions 部署前端及其后端交付物，按仓库现有构建和服务重启方式执行。
- 第三方 API 项目：同样使用 Actions 完成构建和部署；通过环境变量或仓库密钥注入 API 配置，不把密钥写入仓库。
- 检查 `.github/workflows/*.yml` 和 `.yaml` 中所有 Action 的运行时兼容性，优先使用支持 Node.js 24 的稳定版本。`actions/checkout@v4` 会触发 Node.js 20 弃用提示，默认更新为支持 Node.js 24 的版本（当前优先使用 `actions/checkout@v5`），并一并审查其他 Action。
- 保持部署路径、环境、密钥和触发条件可配置；部署前运行构建、测试或必要的静态检查。
- 电脑默认安装 GitHub CLI（`gh`）。每次 `git push` 成功后，先确认 `gh` 可用且已认证，再按当前分支和刚推送的 commit SHA 定位对应的 Actions run，使用 `gh run list`、`gh run view` 或必要时 `gh run watch` 检查状态与日志；不能只看“最新一次”运行或只凭 push 成功判断部署成功。
- Actions 处于 `queued`/`in_progress` 时，报告为部署仍在进行；只有对应 run 进入成功终态，才报告自动部署已确认。失败、取消、跳过或找不到对应 run 时，检查触发条件和日志并报告，未经用户授权不自动 rerun、回滚或 force push。

## 反馈与经验沉淀

- 每次网站任务结束后复盘用户反馈、验收意见、测试结果和实际操作失误；开始下一个网站前检查 [lessons-learned.md](references/lessons-learned.md) 中与当前项目相关的经验。
- 用户明确指出的错误、遗漏或新的长期要求，直接归纳成简洁、可复用的规则，更新到本 Skill 最合适的章节，并在 `references/lessons-learned.md` 追加一条记录。
- 只把可跨项目复用的经验写入默认规范；项目专属配置、一次性临时问题、未经验证的猜测和与现有规则冲突的内容不要直接升级为默认规则。项目专属的设计、计划与验收记录应写入该网站自己的 `docs/`，不要写进本 Skill；只有确认可跨项目复用后才升级到默认规范。
- 对自行发现但尚未确认的问题，先记录为“待确认”经验；当用户确认或后续验证重复出现后，再升级为默认规则。
- 修改 Skill 后运行 `quick_validate.py`，检查规则是否清晰、无重复、无互相冲突；将本次新增或调整内容告知用户。
- 不因为更新 Skill 而创建分支或自动执行 `git push`。

## 交付前检查

- 确认当前分支为 `main`，没有创建或切换分支，也没有未经授权的 `git push`。
- 确认图文卡片和鼠标悬浮反馈存在且符合页面主题。
- 悬浮到带鼠标浮动动效的模块（一般为卡片）时确认显示小手指针，并确认点击区域、链接和键盘操作有效；纯展示且无点击行为的模块确认未误用指针。
- 检查文字行高、段落间距和模块垂直留白；与参考截图相比默认控制在约 50%，没有大面积空白。
- 滚动页面时确认导航持续固定在顶部，并在滚动状态下适度缩小高度；检查桌面端、移动端、锚点跳转和菜单展开不被遮挡。
- 检查顶部主导航固定栏目不超过 7 个，固定栏目（首页 / Home、关于我们 / About、产品展示 / Product 或服务业“服务项目 / Service”、新闻资讯 / News、联系我们 / Contact）中英文标签一一对应，超出栏目已收纳进“关于我们”下拉。
- 检查内页导航下方默认存在约 300px–400px 高度的横幅；除非用户特别说明，所有内页均应有横幅且不与固定导航重叠或遮挡。
- 逐项检查标题、标签、图片、正文、按钮和图标的独立进入动效；确认动效有可读性停顿，没有整块同时运动。
- 检查桌面、平板和移动端的联系方式浮层、触控行为及内容遮挡情况；桌面端确认整组浮动框贴浏览器最右侧，未被内容容器宽度或页面内边距推入页面。
- 在页面顶部确认右侧浮动框底部的返回顶部箭头隐藏，滚动后确认显示、可点击且能平滑返回顶部，箭头随浮动框一起固定且不另起独立组件。
- 检查 `prefers-reduced-motion`、键盘焦点、对比度、图片替代文本和触控尺寸。
- 检查案例数据模型、筛选/分页 URL 恢复、详情页证据资产、外链状态、语言版本和无结果/待补状态；至少抽查一个图片丰富案例和一个内容待补案例。
- 检查控制台没有可修复的 JavaScript 异常、资源失败或第三方库弃用警告；不把“页面能打开”当作案例模块验收通过。
- 检查所有页面的 favicon 是否引用当前项目“建站资料”中以 `favicon` 开头的实际图片；确认选中文件、部署路径、格式和引用方式与项目类型/框架一致，没有误用固定地址、其他项目资源或框架默认资源。
- 如果使用 `assets/qr-placeholder-test.png`，确认它只用于开发/验收占位，有明确的非真实说明且不可点击；生产交付前已替换为项目实际二维码或隐藏该项。
- 检查 AI 生成的中文、英文和中英混排文字在桌面端、平板端和移动端均保持紧凑易读；实际渲染字号没有超过对应语义上限，未通过放大文字解决换行或布局问题。
- 如果本次打开了项目，确认已检查工作区并完成安全的 `git pull --ff-only`，或已记录因本地改动、分叉或冲突而暂停同步的原因。
- 如果本次执行了 `git push`，确认已用 GitHub CLI 检查当前 commit 对应的 Actions run；区分部署进行中、成功、失败、取消、跳过和找不到 run，不把 push 成功直接当作部署成功。
- 如果是 WordPress 客户服务器，确认 Actions 触发前已使用本机对应 SSH 完成主机/用户核对，并记录远端 `rsync`、WP-CLI 版本和预检结果；任何预检失败都不得进入自动部署。
- 检查 Actions 使用 Node.js 24 兼容版本，不再保留会产生该弃用提示的 `actions/checkout@v4`。
- 按项目类型验证 WordPress 目录、自建 API 或第三方 API 的构建与部署路径。
