# 网站制作经验记录

只记录跨项目有复用价值的失误、修正和用户确认过的长期要求。已升级到 `SKILL.md` 的规则仍保留简短记录，项目专属问题不要写入这里。

## 记录格式

使用以下字段追加记录：

- 日期：`YYYY-MM-DD`
- 项目类型：WordPress / 自建 API / 第三方 API / 纯前端
- 问题：实际出现的错误或遗漏
- 修正：采取的处理方式
- 规则状态：已确认 / 待确认
- 默认规则：如果已升级到 `SKILL.md`，写出对应的简短规则

## 已确认经验

- 日期：2026-08-09
  - 项目类型：通用网站
  - 问题：页面动效、卡片交互和部署约定需要统一执行。
  - 修正：建立网站制作默认规范 Skill，并纳入逐元素动效、滚动停顿、卡片小手指针、联系方式浮层、返回顶部和 Actions 部署要求。
  - 规则状态：已确认
  - 默认规则：所有网站任务开始前加载 `website-production-standards`。

- 日期：2026-08-09
  - 项目类型：通用网站
  - 问题：页面文字、段落和模块之间的垂直间距过大，造成大面积空白，类似参考截图中的效果。
  - 修正：收紧行高、段落间距和模块垂直留白，默认按参考截图约 50% 的间距实现。
  - 规则状态：已确认
  - 默认规则：网站默认采用紧凑、易读的行距和模块间距，避免大面积空白。

- 日期：2026-08-10
  - 项目类型：WordPress
  - 问题：容器宽度、移动端内边距和字号需要在不同视口下连续收缩，旧的固定移动端宽度容易与桌面体系脱节。
  - 修正：统一采用 1440px 最大内容宽度、流式总内边距（当前基准为 `clamp(32px, calc(13.52vw - 18.7px), 176px)`）以及 `clamp()` 流式字号；移动端沿用同一套容器公式，并按 1000px、900px、768px 断点收缩布局。
  - 规则状态：已确认
  - 默认规则：网站默认使用最大内容宽度 + 流式总内边距 + 流式字号的连续宽度体系。

- 日期：2026-08-10
  - 项目类型：通用网站
  - 问题：页面滚动后导航不够容易访问，导航高度也没有根据滚动状态收紧。
  - 修正：让主导航固定在顶部，并在滚动状态切换为适度紧凑的高度，同时保留可读性、触控区域和锚点偏移。
  - 规则状态：已确认
  - 默认规则：网站滚动时主导航默认固定在顶部，导航高度可适当缩小。

- 日期：2026-08-11
  - 项目类型：通用网站
  - 问题：个人开发时不需要分支和隔离工作区；手动创建的开发辅助文件及客户资料也不应进入版本控制。
  - 修正：默认直接在 `main` 工作区开发，并在仓库根目录 `.gitignore` 中忽略 Superpowers/Codex 等辅助产物、临时文件，以及根目录下以中文字符开头的资料文件夹。
  - 规则状态：已确认
  - 默认规则：个人开发直接使用 `main`；客户资料和本地开发辅助产物默认通过 `.gitignore` 排除。

- 日期：2026-08-13
  - 项目类型：通用网站
  - 问题：右侧整组浮动框默认应贴浏览器最右侧；原有自适应容器规则在 1440px 视口下的两侧留白不足。
  - 修正：桌面端整组浮动框脱离内容容器并贴 `right: 0`；将流式总内边距调整为 1440px 时 176px，即左右各 88px，同时保留移动端 16px 单侧留白。
  - 规则状态：已确认
  - 默认规则：右侧浮动框默认贴浏览器最右侧；1440px 视口下内容容器左右各留 88px。

- 日期：2026-08-13
  - 项目类型：通用网站
  - 问题：把某一项目的固定 favicon 地址误设为所有网站通用规则，导致无法按各项目的建站资料使用对应 Logo 资源。
  - 修正：改为每个网站默认从自身"建站资料"中选择文件名以 `favicon` 开头的图片，并按项目现有配置和实际用途引用随项目部署的资源；多个候选无法判断时提醒用户确认，缺失时记录并提醒补充，不跨项目借用或使用固定地址兜底。
  - 规则状态：已确认
  - 默认规则：所有形式的网站默认使用自身建站资料中以 `favicon` 开头的图片作为 favicon；没有匹配资源时不得自行猜测替代资源。

- 日期：2026-08-13
  - 项目类型：通用网站
  - 问题：AI 生成的中文和英文文案容易使用过大的字号，造成首屏拥挤、标题溢出或页面信息密度失衡。
  - 修正：统一采用紧凑、易读的语义字号层级，限制普通 H1 在桌面端和移动端的默认上限，并要求通过缩短文案、换行或布局适配文字，不通过放大字号解决问题。
  - 规则状态：已确认
  - 默认规则：AI 生成的中文、英文及中英混排文字默认不要过大，正文保持约 16px 级别，普通 H1 桌面端不超过 36px、移动端不超过 24px。

- 日期：2026-08-13
  - 项目类型：通用网站
  - 问题：打开项目时如果不及时同步远端版本，可能基于过期代码继续开发；直接 pull 又可能覆盖本地未提交改动或引入未处理分叉。
  - 修正：每次打开项目先检查工作区、分支和上游状态；工作区干净且可 fast-forward 时自动执行 `git pull --ff-only`，否则暂停同步并保护本地改动。
  - 规则状态：已确认
  - 默认规则：每次打开项目后自动获取并 pull 最新版本，但不以覆盖本地改动的方式强行同步。

- 日期：2026-08-13
  - 项目类型：通用网站
  - 问题：`git push` 成功只代表提交已发送到远端，不代表 GitHub Actions 自动部署已经成功；忽略 queued、失败或错误 commit 的 run 会导致误报。
  - 修正：电脑默认使用 GitHub CLI；每次 push 后按当前分支和 commit SHA 定位 Actions run，检查状态、结论和日志，区分进行中、成功和失败等终态。
  - 规则状态：已确认
  - 默认规则：每次 push 后使用 `gh` 检查对应自动部署，只有匹配当前 commit 的 Actions run 成功才确认部署完成。

- 日期：2026-08-13
  - 项目类型：WordPress
  - 问题：客户服务器的 SSH 能连通不代表实际部署用户具备 `rsync` 和 WP-CLI，也不代表 GitHub Actions runner 能访问同一环境；先触发 Actions 可能导致部署中途失败。
  - 修正：Actions 部署前使用本机保存且已确认的对应服务器 SSH 做只读预检，核对主机、用户、环境、指纹，并在实际部署用户下验证 `rsync`、WP-CLI 可执行及版本；同时核对 runner 的 SSH、网络和运行条件。
  - 规则状态：已确认
  - 默认规则：WordPress 客户服务器在自动部署前必须完成 SSH、`rsync` 和 WP-CLI 预检；任一项缺失、不可执行或无法核对时阻止 Actions，不输出敏感凭据。

- 日期：2026-08-14
  - 项目类型：通用网站
  - 问题：内页进入正文前缺少统一的视觉过渡区域，页面层级在导航之后衔接生硬。
  - 修正：在主导航（内页导航）下方默认加入高度约 300px–400px 的横幅，承载内页标题、核心引导文案或主题视觉，作为首屏过渡；横幅不与固定导航重叠，文字沿用全站紧凑字号上限。用户特别说明时可省略。
  - 规则状态：已确认
  - 默认规则：内页导航下方默认加约 300px–400px 横幅，除非用户特别说明。

- 日期：2026-08-14
  - 项目类型：通用网站
  - 问题：带鼠标浮动动效的模块没有统一光标规范，容易与"只有卡片才显示小手"的既有规则脱节，导致可点击区域缺少指针反馈，或纯展示模块因动效误用指针造成误导。
  - 修正：明确凡是带有鼠标浮动动效的模块，鼠标移上去默认显示小手指针（`cursor: pointer`），因为动效本身在暗示该元素可交互；这类模块一般以卡片形式出现，也适用于其他带悬浮反馈的可点击区域；纯展示且无点击行为的模块不得为动效而加小手。
  - 规则状态：已确认
  - 默认规则：带鼠标浮动动效的模块默认显示小手指针；纯展示且无点击行为的模块不加误导指针。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：参考 [案例页](https://www.bomin.cn/case) 通过行业分类、独立详情 URL 和分页组织大量项目，但筛选/分页状态、空结果和详情链接容易在实现时被当成纯视觉模块处理。
  - 修正：将案例列表与详情统一为规范化数据模型；筛选和分页状态写入可分享 URL，切换筛选回到第 1 页，并在刷新、后退、直接打开详情 URL 后复核状态与外链。
  - 规则状态：已确认
  - 默认规则：案例筛选、分页和详情 URL 必须可恢复、可分享，并有明确的无结果和失败状态。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：抽查 [星锐医药](https://www.bomin.cn/biomedical/440)、[合肥普瑞眼科](https://www.bomin.cn/services/444)、[安徽迅微](https://www.bomin.cn/Industrial/443)、[埃尔济咨询](https://www.bomin.cn/services/441) 和 [Taloar](https://www.bomin.cn/foreign_enterprise/409) 后发现，同一参考站点的不同案例详情内容完整度差异明显：有的案例有长图、多页面和关键画面，有的只有宣传性说明，缺少可验证的项目展示资产。
  - 修正：发布详情前增加"案例证据"检查，至少准备一个经过确认的关键页面、产品图、流程图、视频或明确交付物；没有证据时标记为待补，不自行编造结果或客户数据。
  - 规则状态：待确认
  - 默认规则：案例详情应同时具备可核验的项目说明和证据资产；项目专属的指标、授权和内容范围仍需客户确认。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：医疗、制造、金融咨询和外贸案例使用了不同的行业语气、语言和视觉证据；直接复制单一案例的渐变、大图或动效会抹平行业差异。参考站点移动端还出现底部业务咨询按钮与客服气泡相互靠近，动效库输出 Lenis `smooth` 弃用警告。
  - 修正：将视觉手段写成"服务于受众与目标的条件规则"，多语言项目保留真实语言与术语；移动端合并固定入口并预留安全间距，按依赖版本清理弃用配置，同时把控制台警告纳入交付检查。
  - 规则状态：已确认
  - 默认规则：案例视觉与交互必须由行业目标、受众、证据和设备状态驱动；移动端入口不得互相遮挡，控制台可修复警告不得带入交付。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：固定的线性流程会让低风险网站小改动变慢，而完全静默又可能跳过验证、授权和安全边界。
  - 修正：加入兼容版 AURA 自适应策略，以复杂度和风险选择 `DIRECT-EXECUTE`、`LITE-CYCLE`、`FULL-CYCLE` 或 `COLLABORATIVE-ITERATION`；保留必要验证、私有思考边界、用户授权和外部写入保护。
  - 规则状态：已确认
  - 默认规则：静默只减少非必要进度消息，不跳过必要验证；自动化仅限高置信、低风险、可逆且已获授权的操作。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：浏览 [海威官网](https://www.hainwei.com/) 时发现，网站动效不只有滚动淡入和鼠标悬浮，还包括 Hero 场景循环、透明导航转实体导航、产品卡片滚动叙事、背景大字视差、进入视口计数、Mega Menu 预览换图，以及移动端对桌面交互的降级。
  - 修正：在网站规范中加入"触发器 → 状态 → 目的 → 可中断 → 降级"的动效模型，补齐滚动、视差、吸顶、计数、点击/按压、导航、轮播/视频、加载反馈和触摸分类，并把键盘、`prefers-reduced-motion`、性能、CLS 和静态回退纳入验收。
  - 规则状态：已确认
  - 默认规则：动效服务于信息层级和交互反馈；任何视觉动效都不能成为内容可见、可操作或完成任务的唯一条件。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：联系方式资料不完整时，普通示例账号、号码、邮箱或未标明用途的测试二维码可能被误认为真实联系入口。
  - 修正：账号、号码和邮箱缺失时隐藏或保留不可操作的待补占位；二维码缺失时统一使用仓库内 `assets/qr-placeholder-test.png`（编码内容为 `test`）作为开发/验收占位，明确标注非真实、不可点击，上线前替换为项目实际二维码或隐藏。
  - 规则状态：已确认
  - 默认规则：不编造真实联系方式；测试二维码只作为明确的可替换占位，不得作为生产联系方式交付。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：各网站的资料文件夹命名不统一，且容易随仓库提交进入版本控制，造成资料外泄或仓库臃肿。
  - 修正：约定每个网站仓库根目录默认存在一个 `建站资料` 文件夹集中存放本站所有资料（素材、文案、图片、favicon 等），并把根目录下以中文字符开头的文件夹（含 `建站资料`）匹配规则加入 `.gitignore`，规则只对尚未纳入版本控制的文件生效。
  - 规则状态：已确认
  - 默认规则：每个网站仓库默认包含根目录 `建站资料` 文件夹并加入 `.gitignore`，集中存放本站资料。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：顶部导航栏目数量、命名和中英文对应关系在不同项目中不统一，栏目过多时横向铺开影响可读性。
  - 修正：默认固定顶部主导航栏目最多 7 个，固定栏目为 首页 / Home、关于我们 / About、产品展示 / Product、新闻资讯 / News、联系我们 / Contact；生产型企业用"产品展示"，服务业改为"服务项目 / Service"；超量或次要栏目收入"关于我们"下拉，多语言站点标签一一对应、结构一致。
  - 规则状态：已确认
  - 默认规则：顶部导航固定栏目最多 7 个，固定栏目中英文一一对应，超出栏目收进"关于我们"下拉。

- 日期：2026-08-15
  - 项目类型：WordPress
  - 问题：WordPress 项目的 PHP 运行版本在开发机、服务器、PHP-FPM、WP-CLI 和 Actions runner 之间可能不一致，导致本地通过、线上报错或反之。
  - 修正：约定 WordPress 项目默认使用 PHP 8.2；开发电脑默认安装 PHP 8.2，线上服务器、WP-CLI 与 runner 也统一在 PHP 8.2；部署预检把远端 `php -v` 与 WP-CLI 报告的 PHP 版本纳入阻止条件，主题、插件和 seed 按 PHP 8.2 边界编写。
  - 规则状态：已确认
  - 默认规则：WordPress 默认 PHP 8.2，开发机与线上环境统一，预检校验远端 PHP 版本不符则阻止部署。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：纯色背景的模块容易显得呆板、缺乏层次，缺少轻量视觉点缀。
  - 修正：模块为纯色背景时默认适当添加粒子动态特效（漂浮粒子、光点、连线等）；粒子默认使用 canvas 实现，纯 CSS 小范围点缀不禁止，但全屏/大量粒子优先 canvas 避免大量 DOM 节点影响性能，并保持克制密度，不遮挡文字与关键信息，在 `prefers-reduced-motion` 下停止或简化。
  - 规则状态：已确认
  - 默认规则：纯色背景模块默认加适当粒子动效（默认 canvas 实现），且尊重减少动效偏好。

- 日期：2026-08-15
  - 项目类型：通用网站
  - 问题：首页轮播图在移动端若直接沿用桌面大图，容易被裁切或拉伸变形，关键内容展示不全。
  - 修正：纯图片轮播在移动端默认等比例缩小整图、不裁切不拉伸；若单独提供手机版轮播图，则按断点切换为手机版，桌面端仍用桌面版；切换不造成布局跳动或横向滚动。
  - 规则状态：已确认
  - 默认规则：首页纯图轮播移动端等比例缩小；有手机版轮播图则用手机版。

- 日期：2026-08-15
  - 项目类型：WordPress
  - 问题：GEO 与 WordPress 对接所需的兼容入口、遗留签名、字段别名、发布权限、远程封面和故障审计分散在项目文档中，制作新 WordPress 站点时容易只接通发布请求，却遗漏最小权限、限流、SSRF 防护和端到端验收。
  - 修正：把 GEO 后台与 WordPress 对接方式的角色、入口、发现/发布流程、MD5 遗留兼容、密钥存储、字段映射、分类校验、封面策略、审计和错误对照整合进 WordPress reference，并区分现有协议事实与新接口安全默认。
  - 规则状态：已确认
  - 默认规则：GEO 后台与 WordPress 对接使用单一插件处理器、低权限专用作者、可轮换签名、限流、字段净化、分类拒绝策略、SSRF 防护和脱敏审计；所有兼容入口必须做公网端到端验收。
